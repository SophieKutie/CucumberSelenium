#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../selenium-grid/bin/selenium-grid" "$@"
  ret=$?
else 
  node  "$basedir/../selenium-grid/bin/selenium-grid" "$@"
  ret=$?
fi
exit $ret
